# Copy to .dev.vars for local Wrangler preview.
# Never commit real credentials or private deployment identifiers.

SOURCE_ACCESS_PASSWORD=
COAST_SESSION_SECRET=

# Required D1 binding name. Configure the actual database through Wrangler/Pages.
COAST_CHAT_DB=

# Optional model provider configuration.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
